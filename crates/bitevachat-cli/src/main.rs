//! Bitevachat CLI client binary.
//!
//! Communicates with the local node via gRPC. Supports wallet management,
//! messaging, contact management, group operations, and node status queries.

fn main() {
    eprintln!("bitevachat-cli: not yet implemented — awaiting Tahap 17");
    std::process::exit(1);
}
